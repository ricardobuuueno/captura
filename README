1. Baixar o projeto do github

    > git clone https://github.com/ricardobuuueno/captura.git

2. Acessar a pasta do projeto no terminal e criar e ativar o ambiente virtual

    > python -m venv capenv
    > .\capenv\Scripts\Activate.ps1 (ou o script mais apropriado para o seu ambiente)

3. Instalar pacotes no ambiente virtual

    > pip install -r requirements.txt

4. Executar script de captura de dados (use a opção -d para salvar os dados no banco de dados SQLite)

    > python .\main.py [-h] [-d]

5. Opções do script

    Captura - TJMG eProc Data Extractor
    Usage: main.py [-h] [-d]
    Options:
    -h, --help Show this help message and exit
    -d, --database Save results to SQLite database

6. Após a execução, o resultado estará em um arquivo chamado output.json (no mesmo diretório da aplicação), e no banco de dados, caso essa opção tenha sido escolhida (o nome padrão do banco de dados é captura.db, também no mesmo diretório da aplicação).
